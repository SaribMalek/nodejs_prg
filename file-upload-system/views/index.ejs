<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>File Upload System</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/header') %>

  <div class="container py-4">
    <div class="row">
      <div class="col-lg-6">
        <div class="card shadow-sm mb-3">
          <div class="card-body">
            <h5 class="card-title">Upload a File</h5>

            <% if (messages.error && messages.error.length) { %>
              <div class="alert alert-danger">
                <%= messages.error.join('<br>') %>
              </div>
            <% } %>
            <% if (messages.success && messages.success.length) { %>
              <div class="alert alert-success">
                <%= messages.success.join('<br>') %>
              </div>
            <% } %>

            <form id="uploadForm" action="/upload" method="post" enctype="multipart/form-data">
              <div class="mb-3">
                <label for="file" class="form-label">Choose file</label>
                <input class="form-control" type="file" id="file" name="file" required>
                <div class="form-text">Allowed: jpg, png, gif, pdf, doc, docx, txt. Max: <%= (maxFileSize/1024/1024).toFixed(2) %> MB</div>
              </div>
              <button id="uploadBtn" type="submit" class="btn btn-primary">Upload</button>
            </form>
          </div>
        </div>

        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title">Uploaded Files</h5>
            <% if (!files || files.length === 0) { %>
              <p class="text-muted">No files uploaded yet.</p>
            <% } else { %>
              <div class="table-responsive">
                <table class="table table-hover">
                  <thead>
                    <tr>
                      <th>Original Name</th>
                      <th>Size</th>
                      <th>Uploaded At</th>
                      <th></th>
                    </tr>
                  </thead>
                  <tbody>
                    <% files.forEach(function(file) { %>
                      <tr>
                        <td><%= file.original_name %></td>
                        <td><%= (file.size/1024).toFixed(2) %> KB</td>
                        <td><%= new Date(file.uploaded_at).toLocaleString() %></td>
                        <td class="text-end">
                          <a class="btn btn-sm btn-outline-primary" href="/download/<%= file.id %>">Download</a>
                          <form class="d-inline deleteForm" action="/delete/<%= file.id %>" method="post" style="display:inline">
                            <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                          </form>
                        </td>
                      </tr>
                    <% }) %>
                  </tbody>
                </table>
              </div>
            <% } %>
          </div>
        </div>
      </div>

      <div class="col-lg-6">
        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title">Info & Notes</h5>
            <p class="small text-muted">
              This demo stores files in <code>/public/uploads</code> and metadata in MySQL.
            </p>
            <ul>
              <li>Change allowed file types in <code>server.js</code>.</li>
              <li>Adjust <code>MAX_FILE_SIZE</code> in <code>.env</code>.</li>
              <li>For production, consider storing files in S3 or other object storage.</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

  <script>
  $(function() {
    // Confirm delete
    $('.deleteForm').on('submit', function(e){
      if(!confirm('Are you sure you want to delete this file?')) e.preventDefault();
    });

    // Client-side file size check before submit
    $('#uploadForm').on('submit', function(e){
      const input = $('#file')[0];
      if (!input.files.length) {
        alert('Choose a file first');
        e.preventDefault();
        return;
      }
      const file = input.files[0];
      const maxBytes = <%= maxFileSize %>;
      if (file.size > maxBytes) {
        alert('File exceeds max size');
        e.preventDefault();
      }
    });
  });
  </script>
</body>
</html>
